import styles from './Unfinished.module.css'

/**
 * 끝까지 가지 않은 선.
 *
 * 구분선이 아니다. 구분선은 판면을 둘로 나누려고 긋는 것이라 반드시 양쪽 끝에
 * 닿는다. 이 선은 판면의 7할쯤에서 그냥 멈춘다.
 *
 * 그 자리에 설명을 붙이지 않는다. «남긴 커피»라고 적는 순간 이것은 선이 아니라
 * 라벨이 되고, 라벨이 붙으면 읽는 사람은 선 대신 글자를 읽는다. 이 기록에서
 * 무언가가 끝까지 완료되지 않은 형태로 나오는 것은 여기가 처음이고, 뒤에
 * 오는 모든 어긋남이 이 선의 길이를 기준으로 읽힌다.
 *
 * 스크롤을 따라 그어진다. 움직임을 줄이기로 한 화면에서는 이미 그어진 채로
 * 있다 — 읽어야 할 것은 그어지는 동작이 아니라 멈춘 자리다.
 */
export function Unfinished() {
  return <span className={styles.unfinished} role="presentation" />
}
